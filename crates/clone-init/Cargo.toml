[package]
name = "clone-init"
version = "0.1.0"
edition = "2021"
description = "Minimal init binary for Clone guest rootfs boot"

[dependencies]
libc = "0.2"

[profile.release]
lto = true
codegen-units = 1
opt-level = "z"
strip = true

[[bin]]
name = "clone-init"
path = "src/main.rs"
